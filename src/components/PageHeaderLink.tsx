import { Link } from "react-router-dom";
import styled from "styled-components";

export const PageHeaderLink = styled(Link)`
  all: unset;
  box-sizing: border-box;
  padding: 10px 20px;
  background: #ffffff;
  box-shadow: 0px 1px 2px rgba(0, 0, 0, 0.08);
  border-radius: 4px;
  cursor: pointer;
  font-size: 13px;
  line-height: 16px;
  white-space: nowrap;
`;
